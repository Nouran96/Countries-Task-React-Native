import { StyleSheet } from "react-native";

export const Shared = StyleSheet.create({
  redirectText: {
    textAlign: "center",
    fontSize: 15,
  },
  mainContainer: {
    padding: 10,
  },
});
